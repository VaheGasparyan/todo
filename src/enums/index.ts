export enum Names {
    inputValue = 'task',
    localStorageName = 'todo',
    all = 'all',
    active = 'active',
    completed = 'completed',
    btnStateLocalSTRG = 'btnState'
}

export enum Index {
    childrenIndex
}