export enum Names {
    inputValue = 'task',
    localStorageName = 'todo',
    all = 'all',
    active = 'active',
    completed = 'completed',
    btnStateLocalSTRG = 'btnState',
    check = 'check',
    close = 'close'
}

export enum Index {
    childrenIndex,
    idIndex
}